import itertools
from grafanalib.dynamic.core import *
from grafanalib.dynamic.dashboard import Dashboard
dashboard = Dashboard(
  title="{{servername}}",
  timezone="browser", 
  tags = "{{grafana_tags}}",
  rows=[
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='CPU',
            span=12
            )
        ]
    ),
    Row(     
        panels=[
            Graph(
                title = "time_active",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_active"), 1s) AS mean_time_active FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_active',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "time_guest",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_guest"), 1s) AS mean_time_guest FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_guest',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "time_guest_nice",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_guest_nice"), 1s) AS mean_time_guest_nice FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_guest_nice',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "time_idle",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_idle"), 1s) AS mean_time_idle FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_idle',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "time_iowait",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_iowait"), 1s) AS mean_time_iowait FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_iowait',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "time_irq",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_irq"), 1s) AS mean_time_irq FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_irq',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "time_nice",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_nice"), 1s) AS mean_time_nice FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_nice',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "time_softirq",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_softirq"), 1s) AS mean_time_softirq FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_softirq',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "time_steal",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_steal"), 1s) AS mean_time_steal FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_steal',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "time_system",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_system"), 1s) AS mean_time_system FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_system',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "time_user",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("time_user"), 1s) AS mean_time_user FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='time_user',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "usage_active",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_active") AS mean_usage_active FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_active',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "usage_guest",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_guest") AS mean_usage_guest FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_guest',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "usage_guest_nice",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_guest_nice") AS mean_usage_guest_nice FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_guest_nice',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "usage_idle",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_idle") AS mean_usage_idle FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_idle',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "usage_iowait",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_iowait") AS mean_usage_iowait FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_iowait',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "usage_irq",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_irq") AS mean_usage_irq FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_irq',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "usage_nice",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_nice") AS mean_usage_nice FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_nice',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "usage_softirq",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_softirq") AS mean_usage_softirq FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_softirq',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "usage_steal",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_steal") AS mean_usage_steal FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_steal',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "usage_system",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_system") AS mean_usage_system FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_system',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "usage_user",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("usage_user") AS mean_usage_user FROM "cpu" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='usage_user',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='DISK',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "free",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("free") AS mean_free FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='free',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "inodes_free",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("inodes_free") AS mean_inodes_free FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='inodes_free',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "inodes_total",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("inodes_total") AS mean_inodes_total FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='inodes_total',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "inodes_used",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("inodes_used") AS mean_inodes_used FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='inodes_used',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "total",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("total") AS mean_total FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='total',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "used",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("used") AS mean_used FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='used',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
                        Graph(
                                title = "system - cpu time",
                                span = 6,
                                dataSource="{{database}}",
                                targets=[
                                      TargetInfluxDB(
                                            query='SELECT mean("used_percent") AS mean_used_percent FROM "disk" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                                            alias='used_percent',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='DiskIO',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "io_time",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("io_time"), 1s) AS mean_io_time FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='io_time',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "iops_in_progress",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("iops_in_progress") AS mean_iops_in_progress FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='iops_in_progress',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "read_bytes",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("read_bytes"), 1s) AS mean_read_bytes FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='read_bytes',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "read_time",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("read_time"), 1s) AS mean_read_time FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='read_time',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "reads",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("reads"), 1s) AS mean_reads FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='reads',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "weighted_io_time",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("weighted_io_time"), 1s) AS mean_weighted_io_time FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='weighted_io_time',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "write_bytes",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("write_bytes"), 1s) AS mean_write_bytes FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='write_bytes',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "write_time",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("write_time"), 1s) AS mean_write_time FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='write_time',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "diskio - writes",
                span = 6,
                dataSource="{{database}}",
                targets=[
                  TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("writes"), 1s) AS mean_writes FROM "diskio" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='writes',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='KERNEL',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "kernel - boot_type",
                span = 6,
                dataSource="{{database}}",
                targets=[
                  TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("boot_time"), 1s) AS mean_boot_time FROM "kernel" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='boot_time',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
            Graph(
                title = "kernel - context_switches",
                span = 6,
                dataSource="{{database}}",
                targets=[
                  TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("context_switches"), 1s) AS mean_context_switches FROM "kernel" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='context_switches',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "interrupts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("interrupts"), 1s) AS mean_interrupts FROM "kernel" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='interrupts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "processes_forked",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("processes_forked"), 1s) AS mean_processes_forked FROM "kernel" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='processes_forked',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='MEM',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "active",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("active") AS mean_active FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='active',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "available",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("available") AS mean_available FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='available',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "available_percent",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("available_percent") AS mean_available_percent FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='available_percent',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "buffered",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("buffered") AS mean_buffered FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='buffered',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "cached",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("cached") AS mean_cached FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='cached',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "free",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("free") AS mean_free FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='free',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "inactive",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("inactive") AS mean_inactive FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='inactive',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "slab",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("slab") AS mean_slab FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='slab',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "total",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("total") AS mean_total FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='total',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "used",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("used") AS mean_used FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='used',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "used_percent",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("used_percent") AS mean_used_percent FROM "mem" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='used_percent',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
            
            
            
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='NET',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "icmp_inaddrmaskreps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inaddrmaskreps"), 1s) AS mean_icmp_inaddrmaskreps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inaddrmaskreps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_inaddrmasks",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inaddrmasks"), 1s) AS mean_icmp_inaddrmasks FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inaddrmasks',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_incsumerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_incsumerrors"), 1s) AS mean_icmp_incsumerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_incsumerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_indestunreachs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_indestunreachs"), 1s) AS mean_icmp_indestunreachs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_indestunreachs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_inechoreps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inechoreps"), 1s) AS mean_icmp_inechoreps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inechoreps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_inechos",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inechos"), 1s) AS mean_icmp_inechos FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inechos',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_inerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inerrors"), 1s) AS mean_icmp_inerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_inmsgs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inmsgs"), 1s) AS mean_icmp_inmsgs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inmsgs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_inparmprobs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inparmprobs"), 1s) AS mean_icmp_inparmprobs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inparmprobs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_inredirects",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_inredirects"), 1s) AS mean_icmp_inredirects FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_inredirects',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_insrcquenchs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_insrcquenchs"), 1s) AS mean_icmp_insrcquenchs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_insrcquenchs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_intimeexcds",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_intimeexcds"), 1s) AS mean_icmp_intimeexcds FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_intimeexcds',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_intimestampreps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_intimestampreps"), 1s) AS mean_icmp_intimestampreps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_intimestampreps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_intimestamps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_intimestamps"), 1s) AS mean_icmp_intimestamps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_intimestamps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outaddrmaskreps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outaddrmaskreps"), 1s) AS mean_icmp_outaddrmaskreps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outaddrmaskreps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_outaddrmasks",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outaddrmasks"), 1s) AS mean_icmp_outaddrmasks FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outaddrmasks',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outdestunreachs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outdestunreachs"), 1s) AS mean_icmp_outdestunreachs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outdestunreachs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_outechoreps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outechoreps"), 1s) AS mean_icmp_outechoreps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outechoreps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outechos",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outechos"), 1s) AS mean_icmp_outechos FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outechos',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_outerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outerrors"), 1s) AS mean_icmp_outerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outmsgs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outmsgs"), 1s) AS mean_icmp_outmsgs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outmsgs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_outparmprobs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outparmprobs"), 1s) AS mean_icmp_outparmprobs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outparmprobs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outredirects",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outredirects"), 1s) AS mean_icmp_outredirects FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outredirects',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_outsrcquenchs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outsrcquenchs"), 1s) AS mean_icmp_outsrcquenchs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outsrcquenchs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outtimeexcds",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outtimeexcds"), 1s) AS mean_icmp_outtimeexcds FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outtimeexcds',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmp_outtimestampreps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outtimestampreps"), 1s) AS mean_icmp_outtimestampreps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outtimestampreps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmp_outtimestamps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("icmp_outtimestamps"), 1s) AS mean_icmp_outtimestamps FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmp_outtimestamps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmpmsg_intype0",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("icmpmsg_intype0") AS mean_icmpmsg_intype0 FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmpmsg_intype0',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmpmsg_intype3",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("icmpmsg_intype3") AS mean_icmpmsg_intype3 FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmpmsg_intype3',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "icmpmsg_outtype3",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("icmpmsg_outtype3") AS mean_icmpmsg_outtype3 FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmpmsg_outtype3',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "icmpmsg_outtype8",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("icmpmsg_outtype8") AS mean_icmpmsg_outtype8 FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='icmpmsg_outtype8',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_defaultttl",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_defaultttl"), 1s) AS mean_ip_defaultttl FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_defaultttl',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_forwarding",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_forwarding"), 1s) AS mean_ip_forwarding FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_forwarding',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_forwdatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_forwdatagrams"), 1s) AS mean_ip_forwdatagrams FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_forwdatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_fragcreates",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_fragcreates"), 1s) AS mean_ip_fragcreates FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_fragcreates',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_fragfails",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_fragfails"), 1s) AS mean_ip_fragfails FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_fragfails',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_fragoks",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_fragoks"), 1s) AS mean_ip_fragoks FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_fragoks',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_inaddrerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_inaddrerrors"), 1s) AS mean_ip_inaddrerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_inaddrerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_indelivers",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_indelivers"), 1s) AS mean_ip_indelivers FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_indelivers',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_indiscards",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_indiscards"), 1s) AS mean_ip_indiscards FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_indiscards',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_inhdrerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_inhdrerrors"), 1s) AS mean_ip_inhdrerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_inhdrerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_inreceives",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_inreceives"), 1s) AS mean_ip_inreceives FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_inreceives',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_inunknownprotos",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_inunknownprotos"), 1s) AS mean_ip_inunknownprotos FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_inunknownprotos',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_outdiscards",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_outdiscards"), 1s) AS mean_ip_outdiscards FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_outdiscards',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_outnoroutes",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_outnoroutes"), 1s) AS mean_ip_outnoroutes FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_outnoroutes',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_outrequests",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_outrequests"), 1s) AS mean_ip_outrequests FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_outrequests',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_reasmfails",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_reasmfails"), 1s) AS mean_ip_reasmfails FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_reasmfails',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_reasmoks",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_reasmoks"), 1s) AS mean_ip_reasmoks FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_reasmoks',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "ip_reasmreqds",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_reasmreqds"), 1s) AS mean_ip_reasmreqds FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_reasmreqds',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "ip_reasmtimeout",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("ip_reasmtimeout"), 1s) AS mean_ip_reasmtimeout FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='ip_reasmtimeout',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_activeopens",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_activeopens") AS mean_tcp_activeopens FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_activeopens',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_attemptfails",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_attemptfails"), 1s) AS mean_tcp_attemptfails FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_attemptfails',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_currestab",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_currestab") AS mean_tcp_currestab FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_currestab',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_estabresets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_estabresets"), 1s) AS mean_tcp_estabresets FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_estabresets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_incsumerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_incsumerrors") AS mean_tcp_incsumerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_incsumerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_inerrs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_inerrs") AS mean_tcp_inerrs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_inerrs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_insegs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_insegs"), 1s) AS mean_tcp_insegs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_insegs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_maxconn",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_maxconn") AS mean_tcp_maxconn FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_maxconn',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_outrsts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_outrsts"), 1s) AS mean_tcp_outrsts FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_outrsts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_outsegs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_outsegs"), 1s) AS mean_tcp_outsegs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_outsegs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_passiveopens",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_passiveopens"), 1s) AS mean_tcp_passiveopens FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_passiveopens',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_retranssegs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("tcp_retranssegs"), 1s) AS mean_tcp_retranssegs FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_retranssegs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_rtoalgorithm",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_rtoalgorithm") AS mean_tcp_rtoalgorithm FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_rtoalgorithm',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_rtomax",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_rtomax") AS mean_tcp_rtomax FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_rtomax',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_rtomin",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_rtomin") AS mean_tcp_rtomin FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_rtomin',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udp_ignoredmulti",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_ignoredmulti"), 1s) AS mean_udp_ignoredmulti FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_ignoredmulti',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udp_incsumerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_incsumerrors"), 1s) AS mean_udp_incsumerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_incsumerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udp_indatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_indatagrams"), 1s) AS mean_udp_indatagrams FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_indatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udp_inerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_inerrors"), 1s) AS mean_udp_inerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_inerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udp_noports",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_noports"), 1s) AS mean_udp_noports FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_noports',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udp_outdatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_outdatagrams"), 1s) AS mean_udp_outdatagrams FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_outdatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udp_rcvbuferrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_rcvbuferrors"), 1s) AS mean_udp_rcvbuferrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_rcvbuferrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udp_sndbuferrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_sndbuferrors"), 1s) AS mean_udp_sndbuferrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_sndbuferrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udplite_ignoredmulti",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_ignoredmulti"), 1s) AS mean_udplite_ignoredmulti FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_ignoredmulti',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udplite_incsumerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_incsumerrors"), 1s) AS mean_udplite_incsumerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_incsumerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udplite_indatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_indatagrams"), 1s) AS mean_udplite_indatagrams FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_indatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udplite_inerrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_inerrors"), 1s) AS mean_udplite_inerrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_inerrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udplite_noports",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_noports"), 1s) AS mean_udplite_noports FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_noports',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udplite_outdatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_outdatagrams"), 1s) AS mean_udplite_outdatagrams FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_outdatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udplite_rcvbuferrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_rcvbuferrors"), 1s) AS mean_udplite_rcvbuferrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_rcvbuferrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "udplite_sndbuferrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udplite_sndbuferrors"), 1s) AS mean_udplite_sndbuferrors FROM "net" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udplite_sndbuferrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row  
            
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='NETSTAT',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "tcp_close",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_close") AS mean_tcp_close FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_close',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_close_wait",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_close_wait") AS mean_tcp_close_wait FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_close_wait',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_closing",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_closing") AS mean_tcp_closing FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_closing',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_established",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_established") AS mean_tcp_established FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_established',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_fin_wait1",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_fin_wait1") AS mean_tcp_fin_wait1 FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_fin_wait1',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_fin_wait2",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_fin_wait2") AS mean_tcp_fin_wait2 FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_fin_wait2',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_last_ack",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_last_ack") AS mean_tcp_last_ack FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_last_ack',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_listen",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_listen") AS mean_tcp_listen FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_listen',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_none",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_none") AS mean_tcp_none FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_none',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_syn_recv",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_syn_recv") AS mean_tcp_syn_recv FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_syn_recv',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "tcp_syn_sent",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_syn_sent") AS mean_tcp_syn_sent FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_syn_sent',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "tcp_time_wait",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("tcp_time_wait") AS mean_tcp_time_wait FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='tcp_time_wait',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "udp_socket",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("udp_socket"), 1s) AS mean_udp_socket FROM "netstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='udp_socket',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Icmp6OutMLDv2Reports",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutMLDv2Reports"), 1s) AS mean_Icmp6OutMLDv2Reports FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutMLDv2Reports',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Icmp6OutMsgs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutMsgs"), 1s) AS mean_Icmp6OutMsgs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutMsgs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Icmp6OutNeighborSolicits",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutNeighborSolicits"), 1s) AS mean_Icmp6OutNeighborSolicits FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutNeighborSolicits',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Icmp6OutRouterSolicits",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutRouterSolicits"), 1s) AS mean_Icmp6OutRouterSolicits FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutRouterSolicits',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Icmp6OutType133",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutType133"), 1s) AS mean_Icmp6OutType133 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutType133',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Icmp6OutType135",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutType135"), 1s) AS mean_Icmp6OutType135 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutType135',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Icmp6OutType143",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Icmp6OutType143"), 1s) AS mean_Icmp6OutType143 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Icmp6OutType143',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IcmpInDestUnreachs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpInDestUnreachs"), 1s) AS mean_IcmpInDestUnreachs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpInDestUnreachs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IcmpInEchoReps",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpInEchoReps"), 1s) AS mean_IcmpInEchoReps FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpInEchoReps',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IcmpInErrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpInErrors"), 1s) AS mean_IcmpInErrors FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpInErrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IcmpInMsgs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpInMsgs"), 1s) AS mean_IcmpInMsgs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpInMsgs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IcmpMsgInType0",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpMsgInType0"), 1s) AS mean_IcmpMsgInType0 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpMsgInType0',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IcmpMsgInType3",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpMsgInType3"), 1s) AS mean_IcmpMsgInType3 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpMsgInType3',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IcmpMsgOutType3",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpMsgOutType3"), 1s) AS mean_IcmpMsgOutType3 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpMsgOutType3',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IcmpMsgOutType8",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpMsgOutType8"), 1s) AS mean_IcmpMsgOutType8 FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpMsgOutType8',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IcmpOutDestUnreachs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpOutDestUnreachs"), 1s) AS mean_IcmpOutDestUnreachs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpOutDestUnreachs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IcmpOutEchos",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpOutEchos"), 1s) AS mean_IcmpOutEchos FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpOutEchos',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IcmpOutMsgs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IcmpOutMsgs"), 1s) AS mean_IcmpOutMsgs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IcmpOutMsgs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Ip6InDelivers",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InDelivers"), 1s), 1s) AS mean_Ip6InDelivers FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InDelivers',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Ip6InECT0Pkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InECT0Pkts"), 1s), 1s) AS mean_Ip6InECT0Pkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InECT0Pkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Ip6InMcastOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InMcastOctets"), 1s), 1s) AS mean_Ip6InMcastOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InMcastOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Ip6InMcastPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InMcastPkts"), 1s), 1s) AS mean_Ip6InMcastPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InMcastPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Ip6InNoECTPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InNoECTPkts"), 1s), 1s) AS mean_Ip6InNoECTPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InNoECTPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Ip6InOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InOctets"), 1s), 1s) AS mean_Ip6InOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Ip6InReceives",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6InReceives"), 1s), 1s) AS mean_Ip6InReceives FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6InReceives',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Ip6OutMcastOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6OutMcastOctets"), 1s), 1s) AS mean_Ip6OutMcastOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6OutMcastOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Ip6OutMcastPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6OutMcastPkts"), 1s), 1s) AS mean_Ip6OutMcastPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6OutMcastPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Ip6OutNoRoutes",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6OutNoRoutes"), 1s), 1s) AS mean_Ip6OutNoRoutes FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6OutNoRoutes',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Ip6OutOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6OutOctets"), 1s), 1s) AS mean_Ip6OutOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6OutOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Ip6OutRequests",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("Ip6OutRequests"), 1s), 1s) AS mean_Ip6OutRequests FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Ip6OutRequests',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpDefaultTTL",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpDefaultTTL"), 1s) AS mean_IpDefaultTTL FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpDefaultTTL',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpExtInBcastOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInBcastOctets"), 1s) AS mean_IpExtInBcastOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInBcastOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpExtInBcastPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInBcastPkts"), 1s) AS mean_IpExtInBcastPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInBcastPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpExtInECT0Pkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInECT0Pkts"), 1s) AS mean_IpExtInECT0Pkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInECT0Pkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpExtInMcastOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInMcastOctets"), 1s) AS mean_IpExtInMcastOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInMcastOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpExtInMcastPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInMcastPkts"), 1s) AS mean_IpExtInMcastPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInMcastPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpExtInNoECTPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInNoECTPkts"), 1s) AS mean_IpExtInNoECTPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInNoECTPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpExtInOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtInOctets"), 1s) AS mean_IpExtInOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtInOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpExtOutBcastOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtOutBcastOctets"), 1s) AS mean_IpExtOutBcastOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtOutBcastOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpExtOutBcastPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtOutBcastPkts"), 1s) AS mean_IpExtOutBcastPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtOutBcastPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpExtOutMcastOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtOutMcastOctets"), 1s) AS mean_IpExtOutMcastOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtOutMcastOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpExtOutMcastPkts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtOutMcastPkts"), 1s) AS mean_IpExtOutMcastPkts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtOutMcastPkts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpExtOutOctets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpExtOutOctets"), 1s) AS mean_IpExtOutOctets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpExtOutOctets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpForwarding",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpForwarding"), 1s) AS mean_IpForwarding FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpForwarding',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpInAddrErrors",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpInAddrErrors"), 1s) AS mean_IpInAddrErrors FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpInAddrErrors',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpInDelivers",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpInDelivers"), 1s) AS mean_IpInDelivers FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpInDelivers',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpInReceives",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpInReceives"), 1s) AS mean_IpInReceives FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpInReceives',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "IpOutDiscards",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpOutDiscards"), 1s) AS mean_IpOutDiscards FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpOutDiscards',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "IpOutRequests",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("IpOutRequests"), 1s) AS mean_IpOutRequests FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='IpOutRequests',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpActiveOpens",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpActiveOpens"), 1s) AS mean_TcpActiveOpens FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpActiveOpens',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpAttemptFails",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpAttemptFails"), 1s) AS mean_TcpAttemptFails FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpAttemptFails',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpCurrEstab",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpCurrEstab"), 1s) AS mean_TcpCurrEstab FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpCurrEstab',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpEstabResets",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpEstabResets"), 1s) AS mean_TcpEstabResets FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpEstabResets',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtDelayedACKLocked",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtDelayedACKLocked"), 1s), 1s) AS mean_TcpExtDelayedACKLocked FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtDelayedACKLocked',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtDelayedACKLost",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtDelayedACKLost"), 1s), 1s) AS mean_TcpExtDelayedACKLost FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtDelayedACKLost',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtDelayedACKs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtDelayedACKs"), 1s), 1s) AS mean_TcpExtDelayedACKs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtDelayedACKs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtPAWSEstab",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtPAWSEstab"), 1s), 1s) AS mean_TcpExtPAWSEstab FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtPAWSEstab',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPACKSkippedSynRecv",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPACKSkippedSynRecv"), 1s), 1s) AS mean_TcpExtTCPACKSkippedSynRecv FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPACKSkippedSynRecv',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPAbortOnClose",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPAbortOnClose"), 1s), 1s) AS mean_TcpExtTCPAbortOnClose FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPAbortOnClose',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPAbortOnData",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPAbortOnData"), 1s), 1s) AS mean_TcpExtTCPAbortOnData FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPAbortOnData',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPAbortOnTimeout",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPAbortOnTimeout"), 1s), 1s) AS mean_TcpExtTCPAbortOnTimeout FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPAbortOnTimeout',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPAutoCorking",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPAutoCorking"), 1s), 1s) AS mean_TcpExtTCPAutoCorking FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPAutoCorking',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPDSACKIgnoredNoUndo",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPDSACKIgnoredNoUndo"), 1s), 1s) AS mean_TcpExtTCPDSACKIgnoredNoUndo FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPDSACKIgnoredNoUndo',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPDSACKOldSent",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPDSACKOldSent"), 1s), 1s) AS mean_TcpExtTCPDSACKOldSent FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPDSACKOldSent',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPDSACKRecv",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPDSACKRecv"), 1s), 1s) AS mean_TcpExtTCPDSACKRecv FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPDSACKRecv',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPDSACKUndo",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPDSACKUndo"), 1s), 1s) AS mean_TcpExtTCPDSACKUndo FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPDSACKUndo',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPDirectCopyFromBacklog",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPDirectCopyFromBacklog"), 1s), 1s) AS mean_TcpExtTCPDirectCopyFromBacklog FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPDirectCopyFromBacklog',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPDirectCopyFromPrequeue",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPDirectCopyFromPrequeue"), 1s), 1s) AS mean_TcpExtTCPDirectCopyFromPrequeue FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPDirectCopyFromPrequeue',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPFastRetrans",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPFastRetrans"), 1s), 1s) AS mean_TcpExtTCPFastRetrans FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPFastRetrans',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPFromZeroWindowAdv",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPFromZeroWindowAdv"), 1s), 1s) AS mean_TcpExtTCPFromZeroWindowAdv FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPFromZeroWindowAdv',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPHPAcks",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPHPAcks"), 1s), 1s) AS mean_TcpExtTCPHPAcks FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPHPAcks',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPHPHits",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPHPHits"), 1s), 1s) AS mean_TcpExtTCPHPHits FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPHPHits',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPHPHitsToUser",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPHPHitsToUser"), 1s), 1s) AS mean_TcpExtTCPHPHitsToUser FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPHPHitsToUser',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPHystartTrainCwnd",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPHystartTrainCwnd"), 1s), 1s) AS mean_TcpExtTCPHystartTrainCwnd FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPHystartTrainCwnd',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPHystartTrainDetect",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPHystartTrainDetect"), 1s), 1s) AS mean_TcpExtTCPHystartTrainDetect FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPHystartTrainDetect',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPKeepAlive",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPKeepAlive"), 1s), 1s) AS mean_TcpExtTCPKeepAlive FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPKeepAlive',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPLossProbeRecovery",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPLossProbeRecovery"), 1s), 1s) AS mean_TcpExtTCPLossProbeRecovery FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPLossProbeRecovery',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPLossProbes",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPLossProbes"), 1s), 1s) AS mean_TcpExtTCPLossProbes FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPLossProbes',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPOFOQueue",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPOFOQueue"), 1s), 1s) AS mean_TcpExtTCPOFOQueue FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPOFOQueue',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPOrigDataSent",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPOrigDataSent"), 1s), 1s) AS mean_TcpExtTCPOrigDataSent FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPOrigDataSent',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPPrequeued",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPPrequeued"), 1s), 1s) AS mean_TcpExtTCPPrequeued FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPPrequeued',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPPureAcks",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPPureAcks"), 1s), 1s) AS mean_TcpExtTCPPureAcks FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPPureAcks',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPRcvCoalesce",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPRcvCoalesce"), 1s), 1s) AS mean_TcpExtTCPRcvCoalesce FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPRcvCoalesce',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPSackMerged",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPSackMerged"), 1s), 1s) AS mean_TcpExtTCPSackMerged FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPSackMerged',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPSackRecovery",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPSackRecovery"), 1s), 1s) AS mean_TcpExtTCPSackRecovery FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPSackRecovery',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPSackShiftFallback",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPSackShiftFallback"), 1s), 1s) AS mean_TcpExtTCPSackShiftFallback FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPSackShiftFallback',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPSackShifted",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPSackShifted"), 1s), 1s) AS mean_TcpExtTCPSackShifted FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPSackShifted',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPSynRetrans",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPSynRetrans"), 1s), 1s) AS mean_TcpExtTCPSynRetrans FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPSynRetrans',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPTimeouts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPTimeouts"), 1s), 1s) AS mean_TcpExtTCPTimeouts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPTimeouts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTCPToZeroWindowAdv",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPToZeroWindowAdv"), 1s), 1s) AS mean_TcpExtTCPToZeroWindowAdv FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPToZeroWindowAdv',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpExtTCPWantZeroWindowAdv",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTCPWantZeroWindowAdv"), 1s), 1s) AS mean_TcpExtTCPWantZeroWindowAdv FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTCPWantZeroWindowAdv',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpExtTW",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(NON_NEGATIVE_DERIVATIVE(mean("TcpExtTW"), 1s), 1s) AS mean_TcpExtTW FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpExtTW',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpInSegs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpInSegs"), 1s) AS mean_TcpInSegs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpInSegs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpMaxConn",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpMaxConn"), 1s) AS mean_TcpMaxConn FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpMaxConn',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpOutRsts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpOutRsts"), 1s) AS mean_TcpOutRsts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpOutRsts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpOutSegs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpOutSegs"), 1s) AS mean_TcpOutSegs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpOutSegs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpPassiveOpens",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpPassiveOpens"), 1s) AS mean_TcpPassiveOpens FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpPassiveOpens',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpRetransSegs",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpRetransSegs"), 1s) AS mean_TcpRetransSegs FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpRetransSegs',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpRtoAlgorithm",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpRtoAlgorithm"), 1s) AS mean_TcpRtoAlgorithm FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpRtoAlgorithm',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "TcpRtoMax",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpRtoMax"), 1s) AS mean_TcpRtoMax FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpRtoMax',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "TcpRtoMin",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("TcpRtoMin"), 1s) AS mean_TcpRtoMin FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='TcpRtoMin',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Udp6IgnoredMulti",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Udp6IgnoredMulti"), 1s) AS mean_Udp6IgnoredMulti FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Udp6IgnoredMulti',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "Udp6InDatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Udp6InDatagrams"), 1s) AS mean_Udp6InDatagrams FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Udp6InDatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "Udp6OutDatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("Udp6OutDatagrams"), 1s) AS mean_Udp6OutDatagrams FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='Udp6OutDatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "UdpIgnoredMulti",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("UdpIgnoredMulti"), 1s) AS mean_UdpIgnoredMulti FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='UdpIgnoredMulti',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "UdpInDatagrams",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("UdpInDatagrams"), 1s) AS mean_UdpInDatagrams FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='UdpInDatagrams',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "UdpNoPorts",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT NON_NEGATIVE_DERIVATIVE(mean("UdpNoPorts"), 1s) AS mean_UdpNoPorts FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='UdpNoPorts',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
                        Graph(
                                title = "system - cpu time",
                                span = 6,
                                dataSource="{{database}}",
                                targets=[
                                      TargetInfluxDB(
                                            query='SELECT NON_NEGATIVE_DERIVATIVE(mean("UdpOutDatagrams"), 1s) AS mean_UdpOutDatagrams FROM "nstat" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                                            alias='UdpOutDatagrams',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row  
            
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='PING',
            span=12
            )
        ]
    ),
    Row(
        panels=[
                        Graph(
                                title = "system - cpu time",
                                span = 6,
                                dataSource="{{database}}",
                                targets=[
                                      TargetInfluxDB(
                                            query='SELECT mean("average_response_ms") AS mean_average_response_ms FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                                            alias='average_response_ms',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "maximum_response_ms",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("maximum_response_ms") AS mean_maximum_response_ms FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='maximum_response_ms',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "minimum_response_ms",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("minimum_response_ms") AS mean_minimum_response_ms FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='minimum_response_ms',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "packets_received",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("packets_received") AS mean_packets_received FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='packets_received',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "packets_transmitted",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("packets_transmitted") AS mean_packets_transmitted FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='packets_transmitted',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "percent_packet_loss",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("percent_packet_loss") AS mean_percent_packet_loss FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='percent_packet_loss',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "result_code",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("result_code") AS mean_result_code FROM "ping" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='result_code',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row  
            
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='PROCESSES',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "blocked",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("blocked") AS mean_blocked FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='blocked',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "dead",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("dead") AS mean_dead FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='dead',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "idle",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("idle") AS mean_idle FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='idle',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "paging",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("paging") AS mean_paging FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='paging',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "running",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("running") AS mean_running FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='running',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "sleeping",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("sleeping") AS mean_sleeping FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='sleeping',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "stopped",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("stopped") AS mean_stopped FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='stopped',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "total",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("total") AS mean_total FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='total',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "total_threads",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("total_threads") AS mean_total_threads FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='total_threads',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "unknown",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("unknown") AS mean_unknown FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='unknown',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "zombies",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("zombies") AS mean_zombies FROM "processes" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='zombies',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
        ] # END panels    
    ), # END row
            
              
            
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='SWAP',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "free",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("free") AS mean_free FROM "swap" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='free',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "in",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("in") AS mean_in FROM "swap" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='in',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "out",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("out") AS mean_out FROM "swap" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='out',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "total",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("total") AS mean_total FROM "swap" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='total',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "used",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("used") AS mean_used FROM "swap" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='used',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "used_percent",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("used_percent") AS mean_used_percent FROM "swap" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='used_percent',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),   
        ] # END panels    
    ), # END row
            
    Row(
        height=Pixels(25),
        panels=[
            TextHeader(
            text='SYSTEM',
            span=12
            )
        ]
    ),
    Row(
        panels=[
            Graph(
                title = "load1",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("load1") AS mean_load1 FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='load1',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "load15",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("load15") AS mean_load15 FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='load15',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "load5",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("load5") AS mean_load5 FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='load5',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
                        Graph(
                                title = "system - cpu time",
                                span = 6,
                                dataSource="{{database}}",
                                targets=[
                                      TargetInfluxDB(
                                            query='SELECT mean("n_cpus") AS mean_n_cpus FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                                            alias='n_cpus',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row
    Row(
        panels=[
            Graph(
                title = "n_users",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("n_users") AS mean_n_users FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='n_users',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),
            Graph(
                title = "uptime",
                span = 6,
                dataSource="{{database}}",
                targets=[
                    TargetInfluxDB(
                        query='SELECT mean("uptime") AS mean_uptime FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                        alias='uptime',
                    )
                ],
                yAxes=[
                    YAxis(format=SHORT_FORMAT, min=None, show=True),
                    YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
            ),

        ] # END panels    
    ), # END row
    Row(
        panels=[
                        Graph(
                                title = "system - cpu time",
                                span = 6,
                                dataSource="{{database}}",
                                targets=[
                                      TargetInfluxDB(
                                            query='SELECT mean("uptime_format") AS mean_uptime_format FROM "system" WHERE "host" = \'{{servername}}\' AND $timeFilter GROUP BY time($__interval) fill(null)',
                                            alias='uptime_format',
                  ),
                ],
                yAxes=[
                  YAxis(format=SHORT_FORMAT, min=None, show=True),
                  YAxis(format=SHORT_FORMAT, min=None, show=True)
                ]
              ),
        ] # END panels    
    ), # END row
  ]
                    )
